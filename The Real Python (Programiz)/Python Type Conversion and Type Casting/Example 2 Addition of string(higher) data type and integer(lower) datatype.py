num_int = 123
num_str = "456"

print("Data type of num_int:",type(num_int))
print("Data type of num_str:",type(num_str))

print(num_int+num_str)






#In the above program,
# We add two variable num_int and num_str.As we can see from the output, we got typeerror.
# Python is not able use Implicit Conversion in such condition.
# However Python has the solution for this type of situation which is know as Explicit Conversion.
